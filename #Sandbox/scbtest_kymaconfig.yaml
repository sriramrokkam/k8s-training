apiVersion: v1
clusters: null
contexts:
- context:
    cluster: scbtest-xhlxpm6g-k16rn4tu
    user: scbtest-xhlxpm6g-k16rn4tu
  name: scbtest-xhlxpm6g-k16rn4tu
current-context: scbtest-xhlxpm6g-k16rn4tu
kind: Config
users:
- name: scbtest-xhlxpm6g-k16rn4tu
  user:
    exec:
      apiVersion: client.authentication.k8s.io/v1beta1
      args:
      - get-token
      - --oidc-issuer-url=https://kyma.accounts.ondemand.com
      - --oidc-client-id=12b13a26-d993-4d0c-aa08-5f5852bbdff6
      - --oidc-extra-scope=email
      - --oidc-extra-scope=openid
      command: kubectl-oidc_login
      env: null
      installHint: |
        kubelogin plugin is required to proceed with authentication
        # Homebrew (macOS and Linux)
        brew install int128/kubelogin/kubelogin

        # Krew (macOS, Linux, Windows and ARM)
        kubectl krew install oidc-login

        # Chocolatey (Windows)
        choco install kubelogin
      interactiveMode: IfAvailable
      provideClusterInfo: false
