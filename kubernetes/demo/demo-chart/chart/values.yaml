image:
  repository: "h.ingress.wdf12.k8s-train.shoot.canary.k8s-hana.ondemand.com/library/kube-terminator"
  pullPolicy: IfNotPresent
  tag: "v1"
  digest: ""

imagePullSecrets: []
# - name: secretName

serviceAccountName: chaoskube
rbac:
  create: false

configuration:
  talkToTheHand: true
  interval: "1m"
  labelSelector: "" # foo=bar
